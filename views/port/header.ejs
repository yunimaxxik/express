<!DOCTYPE html>
<!-- prettier-ignore -->
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Библиотека</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        padding-top: 20px;
        background-color: #f8f9fa;
      }
      .book-card {
        transition: transform 0.2s;
      }
      .book-card:hover {
        transform: scale(1.02);
        box-shadow: 0 8px 20px rgba(0,0,0,0.1);
      }
      .btn-outline-danger {
        --bs-btn-color: #dc3545;
        --bs-btn-border-color: #dc3545;
        --bs-btn-hover-color: #fff;
        --bs-btn-hover-bg: #dc3545;
        --bs-btn-hover-border-color: #dc3545;
      }
      .favorite-star {
        color: gold;
        font-size: 1.2rem;
      }
      .container-custom {
        max-width: 900px;
        margin: 0 auto;
      }
      .nav-link {
        font-weight: 500;
      }
    </style>
  </head>
  <body>
    <div class="container container-custom">
      <nav class="navbar navbar-expand-lg navbar-light bg-light rounded-3 shadow-sm mb-4 p-3">
        <div class="container-fluid">
          <a class="navbar-brand" href="/books">📚 Библиотека</a>
          <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav"
          >
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
              <li class="nav-item">
                <a class="nav-link" href="/books">Все книги</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/books/new">➕ Добавить</a>
              </li>
            </ul>
          </div>
        </div>
      </nav>

      <main>
